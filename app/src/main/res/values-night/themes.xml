<resources>
    <style name="Theme.KitchenNoTomo" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="windowSplashScreenBackground">@drawable/bg_splash</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/icontrang</item>
        <item name="windowSplashScreenAnimationDuration">120</item>
        <item name="postSplashScreenTheme">@style/Theme.KitchenNoTomo</item>

        <!-- fallback Android < 12 -->
        <item name="android:windowBackground">@drawable/bg_splash</item>
    </style>
</resources>